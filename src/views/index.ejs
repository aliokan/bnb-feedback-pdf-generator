<!DOCTYPE html>
<html lang="nl">
  <head>
    <%- include('./partials/head'); %>
  </head>
  <body class="m-24">
    <main>
      <section class="page">
        <h1 class="text-2xl">Gemeenten</h1>
        <ul>
          <% municipalities.forEach((name) => { %>
          <li class="hover:underline">
            <a href="/<%= name %>"><%= name %></a>
          </li>
          <hr />
          <% }) %>
        </ul>
      </section>
    </main>
  </body>
</html>
